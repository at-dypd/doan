$ ->
	$(document).on 'change', ".medicinal-plant-search-selector", ->
		url = $(this).data("url") + "&object_id=" + $(this).val()
		window.location.href = url

	$(document).on 'click', ".button-toggle-header-menu", ->
		$(".navbar").toggle("slow")
	